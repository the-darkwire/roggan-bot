# Discord bot auth token (from the Developer Portal).
TOKEN=

# Discord application ID. Used by `pnpm deploy-commands` to register slash
# commands globally; also read at runtime when constructing voice connections.
CLIENT_ID=
